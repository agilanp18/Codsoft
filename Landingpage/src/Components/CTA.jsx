import React from 'react';
import './CTA.css';

const CTA = () => {
    return (
        <section className="cta">
            <h2>Ready to start?</h2>
            <button>Sign Up Now</button>
        </section>
    );
}

export default CTA;